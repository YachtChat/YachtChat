# This is a basic workflow to help you get started with Actions

name: update_server

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Connect to alphabibber Server and test some bash command
      - name: Setup SSH
        uses: MrSquaare/ssh-setup-action@v1
        with:
          host: www.alphabibber.com
          private-key: ${{ secrets.KEY }}
      #- name: Stop chatserver
      #  run: killall node #sudo killall node
      - name: ls
        run: ls
      #- name: change directory to Git_directory
      #  run: cd /home/git_environment/NotZoom/
      - name: pull form Git
        run: sudo git pull
      - name: npm
        run: cd ./signalingServer/ && sudo npm install && cd -
      - name: Start chatserver
        run: sudo node ./signalingServer/chatserver.js &
    #  - name: executing remote ssh commands using ssh key
    #    uses: appleboy/ssh-action@master
    #    with:
    #      host: ${{ secrets.HOST }}
    #      username: ${{ secrets.USERNAME }}
    #      key: ${{ secrets.KEY }}
    #      port: ${{ secrets.PORT }}
    #      script: |
    #        whoami
